project:
  name: bad_usb_connector_missing_vbus_protection
  version: 0.1.0

libraries:
  - ../../libs/generic

board:
  components:
    J1:
      model: generic.connector.usb2
      part_number: USB2_CONNECTOR
      pins:
        VBUS: usb_vbus
        "D+": usb_dp
        "D-": usb_dm
        GND: gnd
    UESD:
      model: generic.protection.usb_esd_basic
      pins:
        DP: usb_dp
        DM: usb_dm
        GND: gnd
  nets:
    usb_vbus:
      kind: power
      nominal_voltage: 5.0
      powered: true
    usb_dp:
      kind: digital_or_analog
      nominal_voltage: 3.3
    usb_dm:
      kind: digital_or_analog
      nominal_voltage: 3.3
    gnd:
      kind: ground

scenarios:
  - name: usb_connector_missing_vbus_protection
    type: interface_protection
    checks:
      - USB_CONNECTOR_PROTECTION_VALID
    target:
      component: J1
    parameters:
      require_vbus_protection: true
      data_working_voltage_min_V: 3.3
      vbus_working_voltage_min_V: 5.0
