component_id: example.connector.usb2_entry_offset
version: 0.1.0
category: connector
ports:
  VBUS:
    kind: electrical_power
    required: true
  "D+":
    kind: passive
    required: true
  "D-":
    kind: passive
    required: true
  GND:
    kind: electrical_ground
    required: true
usb_connector:
  standard: usb2
  vbus_pin: VBUS
  dp_pin: "D+"
  dm_pin: "D-"
  gnd_pin: GND
  entry_direction_offset_deg: 90.0
model_quality:
  source: generic
  confidence: low
  intended_use:
    - usb_connector_entry_clearance_fixture
  not_valid_for:
    - production_connector_model
